{
  "name": "json_socket",
  "version": "1.0.0",
  "description": "",
  "main": "index.mjs",
  "scripts": {
    "cmake-build": "cd build && cmake -DCMAKE_CXX_COMPILER=clang++ -G \"Unix Makefiles\" ../MathSolver",
    "compile": "cd build && make",
    "setup": "npm run cmake-build && npm run compile",
    "start": "npm run compile && node index.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
