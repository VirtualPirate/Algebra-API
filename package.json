{
  "name": "json_socket",
  "version": "1.0.0",
  "description": "",
  "main": "index.mjs",
  "scripts": {
    "install-dependencies": "sudo apt install make && sudo apt install cmake && sudo apt install clang",
    "cmake-build": "cd build && cmake -DCMAKE_CXX_COMPILER=clang++ -G \"Unix Makefiles\" ../MathSolver",
    "compile": "cd build && make",
    "build": "npm run install-dependencies && npm run cmake-build && npm run compile",
    "start": "node index.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
